import React, { useState } from "react";
import Form from "../components/Form";

const SignIn = () => {
  return (
    <React.Fragment>
      <Form title={"SignIn"} />
    </React.Fragment>
  );
};

export default SignIn;
